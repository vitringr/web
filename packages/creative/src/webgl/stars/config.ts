export const defaultConfig = {
  width: 800,
  height: 800,

  particles: 3200,

  minSize: 0.3,
  maxSize: 2.3,
  minSpeed: 0.00005,
  maxSpeed: 0.00015,
};

export type Config = typeof defaultConfig;
