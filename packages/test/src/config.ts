export namespace Config {
  export const canvasSize = 600;
  export const gridSize = 100;

  export const pins = 60;

  export const minimumGap = 10;

  export const colors = {
    background: "#111111",
    lattice: "#AAAAAA",
  } as const;
}
