export namespace Config {
  export const canvasSize = 800;
  export const gridSize = 100;

  export const pins = 50;

  export const gap = 20;

  export const colors = {
    background: "#111111",
    lattice: "#AAAAAA",
  } as const;
}
